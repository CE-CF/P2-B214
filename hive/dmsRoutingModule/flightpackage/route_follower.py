from .dronecommands import instantiate, the_thread, correct_yaw
from dmsRoutingModule.routingpackage.distanceinmeters import DistanceInMeters
import numpy as np

def get_to_next_point(current_point, next_point):
    x1 = current_point[0]
    y1 = current_point[1]
    x2 = next_point[0]
    y2 = next_point[1]
    
    pass

